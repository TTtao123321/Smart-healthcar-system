<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.hms.dao.UserDao">

    <select id="login" parameterType="map" resultType="java.lang.Integer">
        SELECT id
        FROM users u
        WHERE u.username=#{username}
            AND u.password=#{password}
    </select>

    <select id="selectUserPrivileges" parameterType="int" resultType="java.lang.String">
        SELECT DISTINCT p.permission_name
        FROM users u
         JOIN role r ON JSON_CONTAINS(u.role, CAST(r.id AS CHAR))
         JOIN permission p ON JSON_CONTAINS(r.permissions, CAST(p.id AS CHAR))
        WHERE u.id = #{userId}
         AND u.status = 1
    </select>

    <update id="updatePassword" parameterType="map">
        UPDATE users u
        SET u.password = MD5(CONCAT(
                SUBSTRING(MD5(username), 1, 6),
                #{newPassword},
                SUBSTRING(MD5(username), LENGTH(MD5(username)) - 2, 3)
                             ))
        WHERE u.id = #{userId}
          AND u.password = MD5(CONCAT(
                SUBSTRING(MD5(username), 1, 6),
                #{oldPassword},
                SUBSTRING(MD5(username), LENGTH(MD5(username)) - 2, 3)
                               ))
    </update>
</mapper>